meta {
  name: Enable Success Provider
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/internal/v1/providers/{{successProviderId}}/enable
  body: none
  auth: none
}

tests {
  test("success provider enabled", function() { expect(res.status).to.equal(200); });
}
