meta {
  name: List Providers
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/internal/v1/providers
  body: none
  auth: none
}

tests {
  test("both providers are registered", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array").with.lengthOf(2);
  });
}
