meta {
  name: Enable Fallback Provider
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/internal/v1/providers/{{fallbackProviderId}}/enable
  body: none
  auth: none
}

tests {
  test("fallback provider enabled", function() { expect(res.status).to.equal(200); });
}
