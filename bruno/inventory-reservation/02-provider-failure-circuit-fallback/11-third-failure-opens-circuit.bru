meta {
  name: Third Provider Failure Opens Circuit
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/v1/reservations
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-User-ID: {{userId}}
  Idempotency-Key: external-fallback-003
}
body:json {
  {
    "items": [
      {
        "product_id": "{{externalProductId}}",
        "provider_id": "{{failingProviderId}}",
        "quantity": 1
      }
    ]
  }
}
tests {
  test("third checkout still uses fallback", function() { expect(res.status).to.equal(201); });
}
